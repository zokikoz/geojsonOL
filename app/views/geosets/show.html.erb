<% flash.each do |name, msg| %>
  <%= content_tag :p, msg, id: name %>
<% end %>

<p>
  <strong>Name:</strong>
  <%= @geoset.name %>
</p>

<p>
  <strong>Geojson:</strong>
  <%= @geoset.geojson %>
</p>

<%= link_to 'Edit', edit_geoset_path(@geoset) %> |
<%= link_to 'Back', geosets_path %>
